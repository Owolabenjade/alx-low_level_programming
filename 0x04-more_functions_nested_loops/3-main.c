#include "main.h"

/**
 * main - Entry point of the program
 *
 * Description: This program calls the `print_numbers` function to display a series
 * of numbers, but the specific implementation of `print_numbers` is not included in this code.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	print_numbers(); /* Calls the print_numbers function */
	return (0);
}
