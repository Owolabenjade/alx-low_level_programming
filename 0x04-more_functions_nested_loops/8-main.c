#include "main.h"

/**
 * main - Entry point of the program
 *
 * Description: This program demonstrates the usage of the `print_square` function by calling it with different arguments. The `print_square` function is expected to perform a specific task related to printing square patterns based on the provided argument, controlling the size of the square.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	print_square(2);
	print_square(10);
	print_square(0);
	return (0);
}

