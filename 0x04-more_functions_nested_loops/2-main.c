#include "main.h"
#include <stdio.h>

/**
 * main - Entry point of the program
 *
 * Description: This program demonstrates the usage of the `mul` function
 * by multiplying two integers and printing the results.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("%d\n", mul(98, 1024));
	printf("%d\n", mul(-402, 4096));
	return (0);
}
